class Admin {
    constructor({ id, email, password, mobile }) {
        this.id = id;
        this.email = email;
        this.password = password;
        this.mobile = mobile;
    }
}

module.exports = Admin;
